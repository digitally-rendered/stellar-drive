{
  "name": "pet",
  "version": "2.0.0",
  "description": "Pet entity from OA3 Petstore benchmark (v2 adds microchip_id)",
  "storage": "mongo",
  "collection": "pets",
  "schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["name", "status"],
    "properties": {
      "name": {
        "type": "string",
        "description": "Name of the pet"
      },
      "status": {
        "type": "string",
        "description": "Pet status in the store",
        "enum": ["available", "pending", "sold"],
        "default": "available"
      },
      "category": {
        "type": "string",
        "description": "Category of the pet"
      },
      "tags": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Tags for the pet"
      },
      "photo_urls": {
        "type": "array",
        "items": { "type": "string" },
        "description": "URLs of photos of the pet"
      },
      "microchip_id": {
        "type": "string",
        "description": "ISO 11784/11785 microchip identifier for the pet"
      }
    }
  },
  "indexes": [
    { "fields": ["status"], "unique": false },
    { "fields": ["name"], "unique": false },
    { "fields": ["microchip_id"], "unique": true }
  ]
}
