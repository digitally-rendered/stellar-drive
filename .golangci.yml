version: "2"

run:
  timeout: 5m
  go: "1.24"
  exclude-dirs:
    - generated

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
  exclusions:
    generated: strict

formatters:
  enable:
    - gofmt
