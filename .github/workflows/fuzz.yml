name: Fuzz Testing

on:
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      mode:
        description: "Fuzz mode"
        required: false
        default: "all"
        type: choice
        options:
          - lifecycle
          - positive
          - negative
          - all
      max_examples:
        description: "Max examples for schemathesis"
        required: false
        default: "20"
        type: string

permissions:
  contents: read
  pull-requests: write

jobs:
  fuzz:
    name: Schemathesis Fuzz Tests
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo:7
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ping:1})' --quiet"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    env:
      FUZZ_MODE: ${{ github.event.inputs.mode || 'all' }}
      MAX_EXAMPLES: ${{ github.event.inputs.max_examples || '20' }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Build stellar-drive
        run: go build -o stellar-drive ./cmd/stellar-drive

      - name: Install fuzz requirements
        run: pip install -r benchmarks/fuzz/requirements.txt

      - name: Start benchmark app
        run: |
          STELLAR_MONGO_URI=mongodb://localhost:27017 \
          STELLAR_MONGO_DATABASE=stellar_drive_fuzz_ci \
          ./stellar-drive --config=benchmarks/stellar.yaml run --port 8200 &
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8200/health > /dev/null 2>&1; then
              echo "App healthy after ${i}s"
              break
            fi
            sleep 1
          done

      - name: Run fuzz tests
        env:
          FUZZ_MODE_ARG: ${{ env.FUZZ_MODE }}
          MAX_EXAMPLES_ARG: ${{ env.MAX_EXAMPLES }}
        run: |
          python benchmarks/fuzz/run_fuzz.py \
            --url http://localhost:8200/api/v1 \
            --mode "${FUZZ_MODE_ARG}" \
            --max-examples "${MAX_EXAMPLES_ARG}" \
            --output benchmarks/results/fuzz-ci.json
        continue-on-error: true

      - name: Run GraphQL fuzz tests
        run: |
          python benchmarks/fuzz/run_graphql_fuzz.py \
            --url http://localhost:8200/graphql \
            --mode all \
            --output benchmarks/results/graphql-fuzz-ci.json
        continue-on-error: true

      - name: Run MCP fuzz tests
        run: |
          python benchmarks/fuzz/run_mcp_fuzz.py \
            --cmd "./stellar-drive mcp --config benchmarks/stellar.yaml" \
            --mode all \
            --output benchmarks/results/mcp-fuzz-ci.json
        continue-on-error: true

      - name: Run stream validation tests
        run: go test ./benchmarks/stream/ -run TestStream -v -count=1 || true

      - name: Run stream Go benchmarks
        run: go test ./benchmarks/stream/ -bench=. -benchmem -count=1 || true

      - name: Upload fuzz results
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-results-${{ github.sha }}
          path: benchmarks/results/
          retention-days: 90

      - name: Comment fuzz results on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let results = {};
            try {
              results = JSON.parse(fs.readFileSync('benchmarks/results/fuzz-ci.json', 'utf8'));
            } catch (e) {
              console.log('No fuzz results file found');
              return;
            }

            const body = [
              '## Fuzz Test Results (stellar-drive)',
              '',
              '| Metric | Value |',
              '|--------|-------|',
              `| Mode | \`${results.mode || 'all'}\` |`,
              `| Total Checks | ${results.total || 0} |`,
              `| Passed | ${results.passed || 0} |`,
              `| Failed | ${results.failed || 0} |`,
              '',
              results.failed > 0 ? '**Failed checks:**' : '',
              ...(results.results || [])
                .filter(r => !r.passed)
                .map(r => `- \`${r.mode}/${r.entity}/${r.step}\`: ${r.detail}`),
              '',
              `<sub>Generated by fuzz workflow on ${new Date().toISOString()}</sub>`,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const botComment = comments.find(c =>
              c.body.includes('## Fuzz Test Results (stellar-drive)')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body,
              });
            }
